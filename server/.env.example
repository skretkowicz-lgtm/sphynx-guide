# Copy this file to .env and fill in your own values. Never commit .env.

PORT=3000

# If serving index.html from somewhere other than this server (e.g. opening
# the file directly, or hosting the static site elsewhere), set this to that
# origin so the browser's CORS check allows the request. Leave unset to
# allow any origin.
# ALLOWED_ORIGIN=http://localhost:3000

# SMTP credentials used to send mail. For Gmail: enable 2-Step Verification
# on the account, then create an "App Password" at
# https://myaccount.google.com/apppasswords and use that (not your normal
# password) as SMTP_PASS.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-mailbox@gmail.com
SMTP_PASS=your-app-password

# Where contact form submissions are delivered. Can be the same as SMTP_USER.
CONTACT_TO=your-mailbox@gmail.com
